import { useEffect } from 'react'import { Link, useNavigate } from 'react-router-dom'import {  ArrowRight,  Layers,  BarChart3,  Building2,  Users,  Search,  GitCompareArrows,  Zap,  Network,  BrainCircuit,  Database,  LineChart,  CheckCircle2,  Globe2,  Workflow,  FileSearch,  Compass,  ScanSearch,  Layers3,  ShieldCheck,  ChevronRight,} from 'lucide-react'import PageMetadata from '../components/PageMetadata.jsx'import { Button } from '../components/ui/Button.jsx'import { Card } from '../components/ui/Card.jsx'import { Avatar } from '../components/ui/Avatar.jsx'import Logo from '../components/Logo.jsx'import { cn } from '../lib/utils.js'import { useAuth } from '../providers/AuthProvider.jsx'const USE_CASES = [  { id: 1, title: 'View news feed',          desc: 'Live clustered coverage with sentiment overview',            icon: Layers,         to: '/dashboard' },  { id: 2, title: 'Search articles',         desc: 'Search across events, articles and entities',                icon: Search,         to: '/search' },  { id: 3, title: 'Compare headlines',       desc: 'Side-by-side comparison of how outlets cover the same event', icon: GitCompareArrows, to: '/compare' },  { id: 4, title: 'View clustered events',   desc: 'Browse the full event cluster archive',                       icon: Layers,         to: '/events' },  { id: 5, title: 'Politician sentiment',    desc: 'Entity-level sentiment over time',                            icon: Users,          to: '/entities' },  { id: 6, title: 'Media house report',      desc: 'Per-publisher sentiment reports and trends',                  icon: Building2,      to: '/publishers' },  { id: 7, title: 'Bias dashboard',          desc: 'Comparative media bias analysis',                              icon: BarChart3,      to: '/bias' },  { id: 8, title: 'Developer playground',    desc: 'Run ABSA on any text via the API',                             icon: Zap,            to: '/playground' },]const FEATURES = [  { title: 'Clustered event intelligence', description: 'Compare coverage across outlets and see how the same story is framed.', icon: Layers3 },  { title: 'Entity-centric analysis',      description: 'Track politicians, parties, institutions, and how sentiment shifts over time.', icon: ScanSearch },  { title: 'Bias reporting',               description: 'Summarise sentiment distribution, coverage skew, and source-level variance.', icon: BarChart3 },  { title: 'Knowledge graph',              description: 'Connect actors, outlets, and events into one navigable network of context.', icon: Globe2 },  { title: 'Live ABSA playground',         description: 'Run text through the model and inspect entity sentiment with immediate feedback.', icon: Workflow },  { title: 'Research-ready exports',       description: 'Use the workspace for analysis, briefing, and publication-grade reporting.', icon: FileSearch },]const STATS = [  { value: '12k+', label: 'Articles processed' },  { value: '4.2k', label: 'Entity relations' },  { value: '94%',  label: 'Analysis accuracy' },  { value: '150+', label: 'Daily insights' },]const TIMELINE = [  { date: 'Q1 2025', title: 'Foundation',   text: 'Core ingestion pipeline and basic NER implementation.' },  { date: 'Q3 2025', title: 'Intelligence', text: 'Introduction of ABSA and event clustering algorithms.' },  { date: 'Q1 2026', title: 'Scale',        text: 'Expansion to multiple publishers and knowledge graph integration.' },  { date: 'Q3 2026', title: 'Refinement',   text: 'Advanced bias reporting and research-ready export tools.' },]export default function Landing() {  const { user, signOut, notifyRouteChange } = useAuth()  const navigate = useNavigate()  // Tell the auth provider we're on the public landing route so it  // never auto-resumes a previous session here.  useEffect(() => {    notifyRouteChange('/')  }, [notifyRouteChange])  return (    <div className="min-h-screen bg-[var(--bg)] text-[var(--text)]">      <PageMetadata        title="Vantage — Nepal News Intelligence"        description="AI-powered news intelligence for Nepali publishers. Clustered events, entity sentiment, bias reports."      />      {/* ── Top nav ── */}      <header className="sticky top-0 z-30 border-b border-[var(--border-subtle)] bg-[var(--surface)]/85 backdrop-blur-md">        <div className="mx-auto flex h-16 max-w-[1440px] items-center justify-between gap-4 px-6">          <Link to="/dashboard" className="flex items-center gap-2.5">            <Logo size={36} />          </Link>          <nav className="hidden items-center gap-1 md:flex">            {['Features', 'Use cases', 'Workflow'].map(item => (              <a                key={item}                href={`#${item.toLowerCase().replace(' ', '-')}`}                className="rounded-[var(--radius-md)] px-3 h-9 inline-flex items-center text-sm font-medium text-[var(--text-muted)] transition-colors hover:text-[var(--text)] hover:bg-[var(--surface-muted)]"              >                {item}              </a>            ))}          </nav>          <div className="flex items-center gap-2">            {user ? (              <>                <span className="hidden items-center gap-2 sm:inline-flex h-9 rounded-[var(--radius-lg)] border border-[var(--border)] bg-[var(--surface)] px-3 text-sm font-semibold text-[var(--text)]">                  <Avatar name={user.name} size="xs" />                  {user.name}                </span>                <Button as={Link} to="/dashboard" rightIcon={<ArrowRight size={14} />}>                  Open workspace                </Button>                <Button                  variant="ghost"                  size="sm"                  onClick={() => {                    signOut()                    navigate('/sign-in', { replace: true, state: { signedOut: true } })                  }}                >                  Sign out                </Button>              </>            ) : (              <>                <Link to="/sign-in" className="hidden sm:inline-flex h-9 items-center text-sm font-semibold text-[var(--text-muted)] hover:text-[var(--text)]">                  Sign in                </Link>                <Button as={Link} to="/sign-up" rightIcon={<ArrowRight size={14} />}>                  Create account                </Button>              </>            )}          </div>        </div>      </header>      {/* ── Hero ── */}      <section className="relative overflow-hidden">        <div className="absolute inset-0 -z-10 bg-grid-soft" />        <div className="absolute inset-0 -z-10 bg-gradient-to-b from-transparent via-[var(--brand-50)]/50 to-transparent" />        <div className="mx-auto max-w-[1280px] px-6 py-20 lg:py-28">          <div className="flex flex-col items-center text-center">            <h1 className="h-display max-w-3xl">              See how Nepali publishers cover the same story —{' '}              <span className="bg-gradient-to-r from-[#f59e0b] to-[#fdba74] bg-clip-text text-transparent">                at a glance              </span>            </h1>            <p className="mt-5 max-w-2xl text-base text-[var(--text-muted)] sm:text-lg">              Vantage clusters articles into events, extracts entities, scores sentiment, and surfaces bias patterns              across Nepal's top English news portals — so analysts move from data to insight in seconds.            </p>            <div className="mt-8 flex flex-wrap items-center justify-center gap-3">              <Button as={Link} to="/dashboard" size="lg" rightIcon={<ArrowRight size={16} />}>                Open the workspace              </Button>              <Button as={Link} to="/playground" size="lg" variant="outline" leftIcon={<Zap size={16} />}>                Try the AI playground              </Button>            </div>          </div>          <div className="mx-auto mt-16 grid max-w-3xl grid-cols-2 gap-4 sm:grid-cols-4">            {STATS.map(s => (              <div key={s.label} className="card-elevated p-4 text-center">                <p className="h-lg bg-gradient-to-br from-[#f59e0b] to-[#fdba74] bg-clip-text text-transparent">                  {s.value}                </p>                <p className="mt-1 text-xs text-[var(--text-muted)]">{s.label}</p>              </div>            ))}          </div>        </div>      </section>      {/* ── Use cases ── */}      <section id="use-cases" className="py-20">        <div className="mx-auto max-w-[1280px] px-6">          <div className="mx-auto max-w-2xl text-center">            <p className="eyebrow text-[var(--brand-700)]">Use cases</p>            <h2 className="mt-3 h-display">Eight ways to use Vantage</h2>            <p className="mt-3 text-base text-[var(--text-muted)]">              From a quick news feed check to deep bias audits — every page is built around a real research workflow.            </p>          </div>          <div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">            {USE_CASES.map(uc => {              const Icon = uc.icon              return (                <Link                  key={uc.id}                  to={uc.to}                  className="group card-elevated relative overflow-hidden p-5 anim-fade-up"                >                  <span className="absolute right-3 top-3 text-[10px] font-bold uppercase tracking-wider text-[var(--text-subtle)]">#{uc.id}</span>                  <h3 className="mt-3 text-base font-bold text-[var(--text)]">{uc.title}</h3>                  <p className="mt-1 text-xs leading-relaxed text-[var(--text-muted)]">{uc.desc}</p>                  <span className="mt-3 inline-flex items-center gap-1 text-xs font-semibold text-[var(--brand-600)] opacity-0 transition-opacity group-hover:opacity-100">                    Open <ChevronRight size={12} />                  </span>                </Link>              )            })}          </div>        </div>      </section>      {/* ── Features ── */}      <section id="features" className="bg-[var(--surface-muted)]/50 py-20">        <div className="mx-auto max-w-[1280px] px-6">          <div className="mx-auto max-w-2xl text-center">            <p className="eyebrow text-[var(--brand-700)]">Features</p>            <h2 className="mt-3 h-display">A complete news intelligence stack</h2>          </div>          <div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">            {FEATURES.map((f, i) => {              const Icon = f.icon              return (                <div key={f.title} className="card-elevated p-6 anim-fade-up" style={{ animationDelay: `${i * 0.05}s` }}>                  <span className="inline-flex h-11 w-11 items-center justify-center rounded-[var(--radius-lg)] bg-gradient-to-br from-[var(--brand-50)] to-[var(--purple-50)] text-[var(--brand-600)]">                    <Icon size={20} />                  </span>                  <h3 className="mt-4 text-base font-bold text-[var(--text)]">{f.title}</h3>                  <p className="mt-1 text-sm leading-relaxed text-[var(--text-muted)]">{f.description}</p>                </div>              )            })}          </div>        </div>      </section>      {/* ── Timeline ── */}      <section id="workflow" className="py-20">        <div className="mx-auto max-w-[1280px] px-6">          <div className="mx-auto max-w-2xl text-center">            <p className="eyebrow text-[var(--brand-700)]">Roadmap</p>            <h2 className="mt-3 h-display">From MVP to research-grade</h2>          </div>          <div className="mx-auto mt-12 max-w-3xl">            {TIMELINE.map((step, i) => (              <div key={step.date} className="relative flex gap-6 pb-8 last:pb-0">                <div className="flex flex-col items-center">                  <div className="flex h-9 w-9 items-center justify-center rounded-full bg-[var(--brand-600)] text-sm font-bold text-white">                    {i + 1}                  </div>                  {i < TIMELINE.length - 1 ? <div className="mt-2 w-px flex-1 bg-[var(--border)]" /> : null}                </div>                <div className="flex-1 pb-2">                  <p className="eyebrow text-[var(--brand-700)]">{step.date}</p>                  <h3 className="mt-1 text-lg font-bold text-[var(--text)]">{step.title}</h3>                  <p className="mt-1 text-sm text-[var(--text-muted)]">{step.text}</p>                </div>              </div>            ))}          </div>        </div>      </section>      {/* ── CTA ── */}      <section className="py-20">        <div className="mx-auto max-w-[1280px] px-6">          <div className="bg-brand-gradient relative overflow-hidden rounded-3xl p-10 text-center text-white shadow-2xl md:p-16">            <h2 className="h-display text-white">Ready to see the news differently?</h2>            <p className="mx-auto mt-3 max-w-2xl text-base text-white/80">              Open the workspace and explore live clusters, sentiment and bias reports in seconds.            </p>            <div className="mt-6 flex flex-wrap items-center justify-center gap-3">              <Button as={Link} to="/dashboard" size="lg" className="bg-white text-[var(--brand-700)] hover:bg-white/90" rightIcon={<ArrowRight size={16} />}>                Open workspace              </Button>              <Button as={Link} to="/playground" size="lg" variant="ghost" className="text-white hover:bg-white/15" leftIcon={<Zap size={16} />}>                Try the playground              </Button>            </div>          </div>        </div>      </section>      {/* ── Footer ── */}      <footer className="border-t border-[var(--border-subtle)] bg-[var(--surface)] py-8">        <div className="mx-auto flex max-w-[1280px] flex-col items-center justify-between gap-3 px-6 text-xs text-[var(--text-muted)] sm:flex-row">          <span>© 2026 Vantage · Nepal News Intelligence</span>          <span className="font-mono">v1.0 · distilbert-vantage-v1</span>        </div>      </footer>    </div>  )}

